I have the following application code:

<source_code>
<place_code_here repo="ReactAlgorithmVisualizer"/>
</source_code>

Your task is to analyze screenshot and codebase and provide the next information:
1) what is the tag name of the element, selected in dev tool
2) what is the react component name which renders element, selected in dev tool
3) what is the name of method, which is called when element, selected in dev tool, is clicked
