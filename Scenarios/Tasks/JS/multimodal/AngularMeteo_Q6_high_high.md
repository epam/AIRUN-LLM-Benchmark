I have the following application code:

<source_code>
<place_code_here repo="AngularMeteo"/>
</source_code>

Your task is to provide the following information:
1) name of file, which contains business logic, executed when element marked with red arrow is clicked
2) name of method, which contains business logic, executed when element marked with red arrow is clicked
